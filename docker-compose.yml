services:
  # 单进程一体化：server 同时托管 API 和已构建的 web，访问 http://localhost:3000
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - apps/server/.env
    ports:
      - '3000:3000'
    volumes:
      - uploads:/app/apps/server/uploads

volumes:
  uploads: {}
